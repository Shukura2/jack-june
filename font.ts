import { Lexend, Lobster_Two } from "next/font/google";

export const lexend = Lexend({
  subsets: ["latin"],
  display: "swap",
});

export const lobster_two = Lobster_Two({
  weight: ["400", "700"],
  subsets: ["latin"],
  display: "swap",
});
